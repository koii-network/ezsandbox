const { submission } = require('./submission');
const { audit } = require('./audit');
const { distribution } = require('./distribution');

module.exports = {
  submission,
  audit,
  distribution,
};
